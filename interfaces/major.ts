export interface AcademicMajorsInterface {
  id: string;
  major: string;
  url: string;
  path: string;
}

export interface AcademicPaths {
  name: string;
  emoji: string;
  description: string;
  button: string;
  route: string;
}
